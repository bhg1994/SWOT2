import styled from 'styled-components';
import {
    Layout
} from 'antd';
const {
    Sider
} = Layout;

export const SiderWrapper = styled(Sider)`
    overflow: auto;
    height: 100vh;
    position: fixed;
    @media(max-width: 1024px) {
        
}
`